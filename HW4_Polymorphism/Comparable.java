
public interface Comparable {
	// Returns a 1 if the calling object is "greater than" the argument
	// 			 0 if the calling object is "equal to" the argument
	//			-1 if the calling object is "less than" the argument
	
	int compareTo(Comparable c);
}
